# Variables facultatives référencées par token_env dans la configuration.
# Renseigner ces valeurs dans l’environnement du client MCP, jamais dans Git.

HEALTHCHECKS_API_TOKEN=replace-with-readonly-api-key
